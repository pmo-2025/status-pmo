#
# ===============================
# Do not edit this file directly!
# ===============================
#
# Your changes will be overwritten when the Upptime template updates (by default, weekly)
# Instead, change .upptimerc.yml configuration and the workflows will be generated accordingly.
#
# 🔼 Upptime @v1.41.3
# GitHub-powered open-source uptime monitor and status page by Anand Chowdhary

# * Source: https://github.com/upptime/upptime
# * Docs and more: https://upptime.js.org
# * More by Anand Chowdhary: https://anandchowdhary.com


name: Uptime CI
# Desactivado a propósito (disabled_manually): escaneo-rapido.yml hace ahora
# la detección real cada ~10 min. Este workflow se dejó con el cron puesto,
# y GitHub seguía "intentando" dispararlo cada 30 min pese a estar
# desactivado — 0 jobs ejecutados, pero SÍ mandaba el email de "Run failed:
# No jobs were run" cada vez, spam sin ningún aviso real detrás. Se quitan
# schedule/repository_dispatch; se deja solo workflow_dispatch por si algún
# día hace falta lanzarlo a mano.
on:
  workflow_dispatch:
jobs:
  release:
    name: Check status
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          token: ${{ secrets.GH_PAT || github.token }}
      - name: Check endpoint status
        uses: upptime/uptime-monitor@v1.41.3
        with:
          command: "update"
        env:
          GH_PAT: ${{ secrets.GH_PAT || github.token }}
          SECRETS_CONTEXT: ${{ toJson(secrets) }}
          HTTP_PROXY: ${{ secrets.PROXY_URL }}
          HTTPS_PROXY: ${{ secrets.PROXY_URL }}
      - name: Cerrar issues de webs que ya están online
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GH_PAT || github.token }}
        run: |
          python3 << 'PYEOF'
          import subprocess, glob, json, yaml, os

          repo = os.environ['GITHUB_REPOSITORY']

          # Una sola llamada para todos los issues abiertos: antes se consultaba
          # uno por sitio (357 llamadas, ~3 min del total del job).
          abiertos = subprocess.run(
              ['gh', 'issue', 'list', '--repo', repo, '--state', 'open',
               '--limit', '500', '--json', 'number,labels'],
              capture_output=True, text=True)
          try:
              issues = json.loads(abiertos.stdout or '[]')
          except ValueError:
              print('No se pudo leer la lista de issues abiertos')
              issues = []

          por_slug = {}
          for issue in issues:
              for label in issue.get('labels', []):
                  por_slug.setdefault(label['name'], []).append(issue['number'])

          for path in glob.glob('history/*.yml'):
              try:
                  with open(path) as f:
                      data = yaml.safe_load(f)
              except Exception:
                  continue
              if data.get('status') != 'up':
                  continue
              slug = os.path.basename(path).replace('.yml', '')
              for num in por_slug.get(slug, []):
                  subprocess.run(
                      ['gh', 'issue', 'close', str(num), '--repo', repo,
                       '--comment', 'Cerrado automáticamente: el sitio está online según el último escaneo.'],
                      capture_output=True)
                  print(f'Cerrado issue #{num} ({slug})')
          PYEOF
